# Cloudflare Pages 配置文件
name = "uflag"

# 构建配置
[build]
command = "npm run build"
cwd = ""

[build.environment]
NODE_VERSION = "20"

# Pages 配置
pages_build_output_dir = "dist"

# 兼容性设置
compatibility_date = "2024-01-01"
